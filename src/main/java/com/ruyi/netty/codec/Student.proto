syntax = "proto3";  //版本
option java_outer_classname = "StudentPOJO"; //生成的外部类名，同时也是文件名
//protobuf 使用message管理数据
message Student{ //会在StudentPOJO 外部类生成一个 Student 内部类，是真正发送的pojo对象
  int32 id = 1; // int32(protobuf类型）1表示属性序号，不是值
  string name = 2;

}
